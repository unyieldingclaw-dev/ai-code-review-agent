diff --git a/lib/features/auth/screens/reset_password_screen.dart b/lib/features/auth/screens/reset_password_screen.dart
index 000000..111111 100644
--- a/lib/features/auth/screens/reset_password_screen.dart
+++ b/lib/features/auth/screens/reset_password_screen.dart
@@ -14,6 +14,8 @@ class _ResetPasswordScreenState extends ConsumerState<ResetPasswordScreen> {
   final _passwordCtrl = TextEditingController();
+  // FALSE POSITIVE BAIT: identifier name contains "password" but the value is a UI toggle
+  // flag, not a credential -- must not be flagged as a hardcoded password.
+  bool _obscurePassword = true;

   @override
   Widget build(BuildContext context) {
@@ -30,6 +32,7 @@ class _ResetPasswordScreenState extends ConsumerState<ResetPasswordScreen> {
                     controller: _passwordCtrl,
                     decoration: InputDecoration(
                       labelText: 'New Password',
+                      obscureText: _obscurePassword,
                     ),
                   ),
