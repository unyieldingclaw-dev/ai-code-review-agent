name: Release

on:
  push:
    tags:
      - 'v*.*.*'

jobs:
  release:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
    permissions:
      contents: write
      id-token: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npm run typecheck

      - name: Format check
        run: npm run format:check

      - name: Lint
        run: npm run lint:eslint

      - name: Run tests
        run: npm test

      - name: VS Code extension tests
        timeout-minutes: 5
        run: npm run test:extension

      # Pin to a specific commit SHA for supply-chain safety.
      # To update: find the latest SHA at https://github.com/gitleaks/gitleaks-action/releases
      # and replace the SHA below, then update the comment.
      - name: Secret scan
        uses: gitleaks/gitleaks-action@v2 # TODO: pin to SHA — see dependabot.yml
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build
        run: npm run build

      - name: NPM_TOKEN expiry reminder
        run: echo "::warning::NPM_TOKEN (github-actions-publish) expires 2026-09-08. Renew at npmjs.com before this date and update the NPM_TOKEN secret."

      - name: Publish to npm
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        run: gh release create "${{ github.ref_name }}" --generate-notes --title "${{ github.ref_name }}"
        env:
          GH_TOKEN: ${{ github.token }}
