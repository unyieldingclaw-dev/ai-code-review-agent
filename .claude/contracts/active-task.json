{
  "task": "Build an oracle-backed adversarial-dirty calibration fixture (known-positive recall companion to the adversarial-clean restraint measurement) and extend calibration/calibrate.ts to tag findings against it, up to the agreed checkpoint. Approved with 5 adjustments: (1) trap is separate from the 4 planted defects, not a 5th defect; (2) scoring rules pre-registered, not just the oracle; (3) full run configuration (model digests, timeout, sampling disclosure) locked before any trial; (4) the optional totalChunks-shaped contract-bug category excluded since upstream reachability isn't established; (5) raw-output retention reuses the smallest existing mechanism (calibrate.ts's append pattern) rather than a new logging subsystem. Do NOT run any of the three models against this fixture past a mechanical smoke test until the user signs off on the fixture, oracle, scoring rules, and locked run configuration at the checkpoint. Do not change the production model or tune any model-specific prompts/guards during this work.",
  "status": "complete",
  "completion_notes": "N=20x3 scored batches run in the locked order (devstral:latest, qwen3.5:9b, ornith-1.5:9b), manually classified against the pre-registered oracle, audit files written. Headline: mean exact-recall Devstral 20%, Qwen3.5 32.5%, Ornith 52.5% -- reverses the restraint measurement's ranking (Ornith was worst on adversarial-clean, best here). D3 (ms/seconds unit mismatch) was missed by all 60 trials -- a fixture design limitation, not a model signal; every model converged on a different, real, unplanted array-length-validation observation instead. Full results in docs/superpowers/plans/2026-09-18-acr-adversarial-dirty-fixture-design.md's Results section. Superseded by the next contract (historical-corpus checkpoint pair, #84/#85), written on a fresh branch (chore/model-recall-calibration) off main -- this work never belonged on PR #85's own branch and has been moved off it.",
  "created_at": "2026-09-18T21:00:00Z",
  "expires_at": "2026-09-19T05:00:00Z",
  "scope": [
    { "file": "calibration/fixtures/adversarial-dirty.diff", "op": "create" },
    { "file": "calibration/fixtures/adversarial-dirty.oracle.json", "op": "create" },
    { "file": "calibration/calibrate.ts", "op": "edit" },
    { "file": "docs/superpowers/plans/2026-09-18-acr-adversarial-dirty-fixture-design.md", "op": "create" }
  ],
  "checkpoint": "Present the completed fixture, oracle, scoring rules, and locked run configuration to the user for explicit sign-off BEFORE running any of the three models (devstral:latest, ornith-1.5:9b, qwen3.5:9b) against calibration/fixtures/adversarial-dirty.diff for a scored N=20 trial. A single mechanical smoke-test invocation (to confirm the new calibration case executes end-to-end) is permitted and does not count as a scored trial or require prior sign-off, but must be disclosed."
}
